# Development Profile Configuration
spring:
  # MySQL Database for development (XAMPP)
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/mobai_warehouse?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:mobai}
    password: ${DB_PASSWORD:mobai123}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    org.example.backend: DEBUG
